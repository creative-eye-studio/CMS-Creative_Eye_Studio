{% extends 'admin.html.twig' %}

{% block title %}Style du site{% endblock %}

{% block body %}

<section class="bloc1">
    <p class="btn-add">
        <a class="btn-link" href="{{ path('add_article') }}">Ajouter un article</a>
    </p>
    <h2>Style SCSS du site</h2>
    {{ form_start(form) }}
        {{ form_row(form.code_front, {
            label: 'Code source du CSS'
        }) }}
    {{ form_end(form) }}
</section>

{% endblock %}